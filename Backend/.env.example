# Prefer the project-root `.env`. This file is optional and overrides root values.
# See ../../.env.example for the full list of variables.

DATABASE_URL=postgresql://DB_USER:DB_PASSWORD@localhost:5432/WBP_DB
